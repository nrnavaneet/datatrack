# Examples

[← Documentation home](../docs/README.md)

- **`sample_snapshot_minimal.yaml`** — smallest valid shape the diff and verify commands expect for `tables` (and empty collections for optional sections). Tables include an explicit empty `indexes` list to mirror real tracker output.
- **`workflow.md`** — suggested order of CLI commands from `init` through `pipeline run`.
