# Examples

[← Documentation home](../docs/README.md)

- **`sample_snapshot_minimal.yaml`** — smallest valid shape the diff and verify commands expect for `tables` (and empty collections for optional sections). Use it as a template when writing fixtures or documentation snippets.
- **`workflow.md`** — suggested order of CLI commands from `init` through `pipeline run`.
